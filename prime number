n=int(input("enter the number"))
def prime(n):
	count=0
	for i in range (1,n+1):
		if n%i==0:
			count+=1
	if count==2:
		print("prime number")
	elif n==1 or n==0:
		print("not a prime")
	else:
		print("not a prime")
prime(n)
